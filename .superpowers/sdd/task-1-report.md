# Task 1 Report: Add `firebase-admin` dependency

## What I implemented
Added `"firebase-admin>=7.0.0"` to the `dependencies` list in `pyproject.toml`, after `"python-telegram-bot>=21.0"`.

## What I tested and test results
- Ran `uv sync` — installed firebase-admin 7.5.0 with all transitive dependencies (google-cloud-firestore, grpcio, etc.) successfully. No errors.

## Files changed
- `pyproject.toml` — added firebase-admin to dependencies
- `uv.lock` — updated lockfile (auto-generated by uv sync)

## Self-review findings
None. Straightforward dependency addition.

## Commit
- `8c0f559` — `deps: add firebase-admin for RTDB storage backend`
